// .foreach = this method is used to loop over an arrayby executing providd callback function for an each element of array

const arr = [1, 2, 3];

arr.forEach(element => {
    console.log(element);
})